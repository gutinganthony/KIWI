# LEARNINGS — 歷任 session 踩坑紀錄

> 每個 session 開始時讀本檔。格式與精簡規則見 agents/MAINTENANCE.md §3–§4。
> 已固化進制度檔的教訓不在此重複。上次精簡：2026-09-24（37→12 條；原文備份 agents/backups/LEARNINGS.md.2026-09-24.bak）。
> 舊引用「LEARNINGS <日期>」對照：網路可達性與行情/開示端點（原 07-10 WebFetch、07-26 Yahoo／日本站 403、08-02 備援鏈、08-09 TDnet、08-13 FinMind、08-21 EDINET、08-26 NKD／yrhiprice）→ agents/DISPATCH.md §8；自訂 agent 載入與約束（07-07、07-15）、claude-code-remote 工具（07-12）→ DISPATCH §2；WebSearch 上限（07-26）→ DISPATCH §1；Bash cwd（07-07）→ DISPATCH §5；持倉真實來源（08-20）→ CLAUDE.md §0。合併條目在「（含 …）」保留原日期。

- [2026-07-10] 情境：回覆語言｜事實：Jake 要求對他的所有回覆一律用繁體中文（除非他另外指定）；曾漂移成英文被糾正｜規則：全程繁中，程式碼/指令/專有名詞可保留英文，不因模型切換或上下文語言漂移。
- [2026-08-13] 情境：時間敏感分析的「今天」｜錯誤：把使用者上傳 CSV 檔名時戳（20260729）當今天並寫進派工 prompt，得出「全類股跌停、不要進場」——實際是 8/13，7/29 是波段低點，之後族群反彈 24–73%｜規則：系統 context 的 currentDate 是權威；時間敏感分析第一步用 API 確認「最新可得交易日」（如 FinMind 查 2330 的 max date），不從檔名、使用者敘述或先前訊息推斷；派 agent 不寫死日期，要它先確認並回報今天日期。
- [2026-08-26] （含 07-26、08-02、08-09）情境：需要精確價格/市值/P/E 的任務（週報重定價、觸發判定）｜錯誤：07-22 週報用 WebSearch snippet 當主源，9 檔價格誤植並產出假觸發｜事實：Yahoo API 自 08-02 起在 runner 全 429（`agents/loops/weekly-repricing-audit.md` 仍寫它為主源＝過期）；「雲端擋」和「runner 擋」是兩件事，封鎖結論不可跨環境沿用｜規則：價格類一律走 DISPATCH §8 的實測資料源鏈——開工先用一檔（如 MU）試打確認沒被限流，並以「上週基準收盤 vs repo 快照逐檔比對」驗收；WebSearch 只作交叉驗證與「為什麼動」的敘事；日股財報數字以 TDnet 決算短信原文為主源；多日收盤判定不可只靠 CNBC（只有兩個價格點）。
- [2026-08-21] （含 07-26、07-27、08-19）情境：把雲端抓不到的資料搬進 Actions 資料橋（端點見 DISPATCH §8）｜錯誤：①落地的 .md 沒被任何 `git add` glob 涵蓋，隨 runner 回收消失一整天沒人發現 ②檔案有進 repo，內容卻只有兩行 `HTTPError: 403`（irbank），沉默失敗三週｜正解：雲端擋→先問 runner 抓不抓得到（會重複用到才搬）；新來源先寫 `--probe` 一次列出 N 個候選的 HTTP 狀態＋body 片段，並放一個已知可達的對照組；回 200 的來源先存 HTML 前 1500 字當樣本，沒看過真實 DOM 不寫解析器；清單型解析器要輸出「解析列數／命中列數」；無資料時產出檔明寫「連續為空先查可達性，不要當成沒有資料」｜規則：新產出路徑立刻確認被某個 `git add` glob 涵蓋（狀態檔也要符合 glob，例如該目錄只收 `*.md`）；驗收看「repo 裡有沒有非空的真實內容」，不是「腳本有沒有跑」；使用者卡在第三方金鑰註冊時記得「需要金鑰」是 API 的性質不是資料的性質，先探測免金鑰的網頁/鏡像路徑。
- [2026-07-30] （含 07-10×3、07-12）情境：寫會 commit-back 的 GitHub Actions｜事實與正解：①commit 訊息任何位置出現字面 [skip ci]／[ci skip] 都會讓 GitHub 跳過觸發（要描述就寫「skip-ci 標記」）②CI 產物落地前先估大小，>1MB 先在 runner 上消化成摘要再入版控（曾把 36MB 原始回應 commit 進 repo）③多支 workflow 寫同分支，push 前一律 `git pull --rebase origin "${GITHUB_REF_NAME}"`（否則被拒、報告隨 runner 消失）④高頻（如 10 分鐘）workflow 的狀態走 `actions/cache/restore`＋`save`（key 帶 `github.run_id`、restore-keys 用前綴），權限降 `contents: read`，並設計成「cache 遺失＝只重建基準、不推播」⑤遠端 session 可直接 push 含 `.github/workflows/` 的 commit，但 cron 只在 default branch 生效｜規則：新增排程 workflow 要提醒使用者「合併 main 才會開始跑」；改 workflows 本身仍須先問使用者（CLAUDE.md §0）。
- [2026-07-30] （含 07-28）情境：在工作分支上看資料新鮮度／開長命 PR｜事實：main 每天被排程自動 commit 多次（dashboard/monitor/funnel）→ ①工作區資料時間戳落後 main（曾誤報 tw-funnel 停更 3 天，實為分支落後 12 個自動 commit＋週末）②PR 夾帶 CI 會寫的路徑就會衝突不斷（PR #31 累積 50 個），解完又被下一個自動 commit 作廢｜正解：判斷新鮮度前 `git fetch origin main` 再 `git log --oneline -3 origin/main -- <路徑>`，並先排除週末/非交易日；PR 只含程式碼（丟掉分支上的資料 commit 後 force-with-lease），資料交給 main 的排程重生｜規則：長命 feature branch 的 PR 絕不夾帶 `*/data/**`、`docs/**` 等 CI 寫入路徑；不要逐次解衝突。
- [2026-08-19] （含 07-07）情境：本容器的 Python 環境｜事實：①直接 `pip install` 撞 debian 系統套件衝突（PyJWT RECORD file not found）②系統 `cryptography` 損壞（缺 `_cffi_backend`）時，import 含原生擴充（pyo3/rust/cffi）的套件會拋 `PanicException`——它繼承 `BaseException`，`except Exception`／`except ImportError` 都攔不住（宿主 `fetch_backtest_ext.run_jp_bridge()` 的守衛只有 `except Exception`，漏過去就打掛每日 dashboard job）｜規則：pip 一律 `--user`；side-mount／best-effort 腳本裡 import 或呼叫原生擴充套件一律 `except BaseException` 並快取結果。
- [2026-08-17] （含 07-30）情境：解析 Hyperliquid 成交（projects/hyper-observer/classify.py）｜事實：①`parse_fills` 的 `_norm_ts` 已把時間正規化成 epoch **秒**（API 原始是毫秒），用毫秒運算曾讓掃描永遠回 0 個候選 ②同一毫秒的多筆成交，陣列順序≠撮合順序（0x8bae35 的 1991 次相鄰 startPosition 比對有 1799 次對不上）｜正解：同毫秒視為不可分批次，淨變化＝signed delta 加總；批次進入前的部位用鏈頭回推（沒被批次內任何 end 值蓋到的 start 值），每批獨立回推、不沿用前批累加（對強平/ADL 自癒），見 `aggregate_round_trips`｜規則：重用既有 parser 先確認輸出單位/型別，不用上游 API 的單位反推；用 startPosition 類欄位重建部位時不信同時間戳的順序；驗收＝重建損益加總與原始 closedPnl 加總分毫不差；離線測試必含「應該通過」的正向案例。
- [2026-07-10] 情境：從交易流（Polymarket activity feed）重建錢包已實現 PnL｜錯誤：recovered(SELL+REDEEM)−cost(BUY) 得出 300%+ ROI、100% 勝率的假獲利｜事實：輸的部位不產生 REDEEM（系統性只看得到贏家）；高頻錢包 activity 1500 筆上限只涵蓋約 12h，「只結算無買入」的市場被當成 cost=0 純利｜正解：只算買入＋出場都在視窗內的完整 round-trip；截斷＋短窗＋結算全贏時工具要明白拒答（reliable=False）｜規則：凡從交易流重建績效，先問「輸家看得到嗎、視窗夠長嗎」；唯一可信 PnL 來源是 user-pnl-api 曲線。
- [2026-08-19] （含 07-27）情境：寫含數值門檻、會觸發真實動作的產出（出場條件、觸發判定、警報閾值、前置閘門）｜錯誤：①出場手冊 v1 有三處「聽起來合理」但無出處（村田受注残比 <1.0 憑空生成、行號錯置、全檔查無的措辭）②憑目測把「mac-manual-homework 積欠 ≤1 項」寫成閘門，實測 `grep -c '^- \[ \]'` 是 46 項＝永遠紅燈｜正解：派 fresh-context verifier 逐條核對引用；自訂數字保留但就地標「自訂、無出處、待確認」（pokerlab 的 🔧 標記即源自此）；門檻改綁可達的具名單項｜規則：這類產出落地後一律派 verifier 逐條驗引用；自己加的量化修飾詞（「連兩季」「跌破 X」）就地標自訂；寫門檻前先實測當下數值，並設在努力可達處——不可達的門檻等於自我豁免。
- [2026-08-09] 情境：Serenity 比對價格 vs 觸發價｜錯誤：把觸發帶當常數，一路沿用 watchlist 寫死的價格｜事實：觸發帶多半是 forward P/E × EPS 指引，而 EPS 指引每季被公司改寫——08-09 那週 Yamaichi「15×≈¥6,700–7,200」用新 EPS 重算實為 ¥8,368（差 16–25%，直接決定觸發響不響）、Kokusai 出現「價格沒到但估值早到」、Kohoku 實為 ¥4,457｜規則：每月第一個週日全掃時，用最新公司指引 EPS 重算全部價格型觸發帶，並在 watchlist 標註「依據的 EPS 指引版本日期」；財報週遇上修/下修，該檔當週重算。forward P/E 分兩類：公司決算短信指引換算的標 `[推論-公司指引]`（可作觸發依據）；資料商估值（Yahoo forwardPE、CNBC 日股 pe）標 `[推論]`（不可）。
- [2026-08-26] （含 08-02×2、08-09）情境：崩跌/暴漲週的宏觀判讀與歸因｜事實：①07-29 週 7 檔同時 🟢，判為同源、先等 → 7/31 V 型反彈後其中 3 檔的觸發依據自己消失 ②07-29 歸因漏了 7/28 熊本地震（日系跌最凶，全球因子解釋不了地域差異）③「記憶體現貨崩跌」的標題，查 TrendForce 一手其實是歐洲零售 DDR5 通道價在跌、合約價續漲 ④08-23 把一週資金流寫成分配面結構讀數，一週後被自己的資料證偽｜規則：同週 ≥3 檔觸發命中先問是否同一宏觀因子——是就當一個部位配、等下一個實體讀數（財報/營收）再執行；歸因清單要能解釋各市場漲跌幅差異，不能就標「歸因不完整」去找地區因子（天災、匯率、當地政策、指數調整）；價格類看空論據要「哪一層價格（合約/現貨/零售）＋一手來源＋連續幾週」三件齊備，否則標 unverified、不作賣訊；單一市場極端漲跌要跨市場同日比對；「同源共同因子」升格為結構讀數需橫跨至少兩個觀察週期且方向一致，只有一週最多記成「輪動」。
