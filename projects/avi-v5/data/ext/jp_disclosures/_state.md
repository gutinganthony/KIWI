# _state — jp_disclosures 資料橋狀態（機器讀寫，勿手改）

> 更新：2026-08-29T08:42:56+00:00。**存成 .md 是刻意的**——`update-dashboard.yml` 的 commit 段
> 只 `git add .../jp_disclosures/*.md`，寫成 .json 會不進版控（LEARNINGS 2026-07-27）。

```json
{
 "disclosures": {},
 "metrics": {}
}
```
