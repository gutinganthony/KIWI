# gstack runtime state (contains daemon tokens/PIDs — never commit)
.gstack/
__pycache__/
*.pyc

# hyper-watch 哨兵的本地狀態（正式執行時走 GitHub Actions cache，不進 repo）
projects/hyper-observer/watch/state/
projects/surge-scan/cache/
